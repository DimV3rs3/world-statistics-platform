<?php
/**
 * /countries page — same as archive but accessed via shortcode page.
 *
 * @package WorldStatPlatform
 */

if ( ! defined( 'ABSPATH' ) ) exit;

// Reuse archive template logic
include WSP_TEMPLATES_DIR . 'archive-wsp_country.php';
